<?xml version="1.0" encoding="utf-8"?>

<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:id="@+id/youtube_row_card_view"
    android:layout_height="wrap_content"
    android:layout_marginLeft="@dimen/card_view_margin_left"
    android:layout_marginTop="@dimen/card_view_margin_top"
    android:layout_marginBottom="@dimen/card_view_margin_bottom"
    app:contentPadding="@dimen/card_view_content_padding">

    <!-- youtube thumbnail view to show the thumbnail of videos -->
    <com.google.android.youtube.player.YouTubeThumbnailView
        android:id="@+id/video_thumbnail_image_view"
        android:layout_width="@dimen/video_thumbnail_image_view_width"
        android:layout_height="@dimen/video_thumbnail_image_view_height"
        android:background="@android:color/darker_gray"
        android:contentDescription="@string/thumbnail_image_view_desc"
        android:scaleType="centerCrop" />


</androidx.cardview.widget.CardView>